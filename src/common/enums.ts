export enum DocumentType {
  RESUME = 'RESUME',
  ID_CARD = 'ID_CARD',
  CERTIFICATE = 'CERTIFICATE',
  LICENSE = 'LICENSE',
  PASSPORT = 'PASSPORT',
  OTHER = 'OTHER',
}

export enum UserRole {
  ADMIN = 'ADMIN',
  COACH = 'COACH',
  CLIENT = 'CLIENT',
}

export enum UserStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  PENDING = 'PENDING',
}
